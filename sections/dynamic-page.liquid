 {% render section %}

  
  {% schema %}
  {
    "name": "t:sections.main-page.name",
    "tag": "section",
    "class": "spaced-section",
    "settings": [
        {
          "type": "text",
          "id": "snippet",
          "label": "snippet"
        }
    ]
  }
  {% endschema %}
  {%- comment -%}

This file has been rewritten to preserve the original behavior.

---
Changes:
- from: "{% render section.settings.snippet %}"
  to: "{% render section %}"
  position: 1..21
{%- endcomment -%}